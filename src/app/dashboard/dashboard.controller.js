(function() {
  'use strict';

  angular.module('localsense.dashboard')
  .controller('DashboardCtrl', ['$state', '$ionicPopup', '$log', 'AuthService', DashboardCtrl]);

  function DashboardCtrl($state, $ionicPopup, AuthService) {
    var dashboard = this;

    
  };

})();